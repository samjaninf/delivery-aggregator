import React from "react";
import Orders from "./Orders";

const Layout = () => (
  <div>
    <nav>
      <h1>Delivery Aggregator</h1>
    </nav>
    <div>
      <Orders />
    </div>
  </div>
);

export default Layout;
