extend type Query {
  me: User @auth @protect(suppressErrors: true)
}

extend type Mutation {
  login(data: LoginInput): User
  logout: LogoutResponse! @protect
}

input LoginInput {
  username: String!
  password: String!
}

type LogoutResponse {
  status: String!
  message: String
}
