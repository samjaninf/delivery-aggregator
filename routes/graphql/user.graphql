extend type Query @protect {
  users: [User!]! @paginate(type: "paginator")
  user(id: ID @eq): User @find
}

type User {
  id: ID!
  name: String!
  email: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  stores: [Store!]!
  abilities: [String!]!
}
